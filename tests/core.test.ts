// tests/core.test.ts
import WebSocketClient from '../src/core';

// describe('WebSocketClient', () => {
//   test('should connect and send messages', () => {
//     const client = new WebSocketClient({ url: 'ws://example.com' });
//     client.on('open', () => {
//       expect(client).toBeDefined();
//     });
//   });

//   test('should handle reconnect', () => {
//     const client = new WebSocketClient({ url: 'ws://example.com' });
//     client.on('close', () => {
//       expect(client).toBeDefined();
//     });
//   });
// });
